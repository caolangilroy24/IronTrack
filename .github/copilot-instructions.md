# Copilot Constraints

- Always prioritize Vue 3 Composition API context inside `<script setup lang="ts">`.
- Enforce layout configuration using `<template>` above `<script>`.
- Use Quasar theme tokens for background styling. Highlight colors must match deep orange utilities (`text-deep-orange`, `bg-deep-orange`).
